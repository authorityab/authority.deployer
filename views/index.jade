extends layout

block content
  h1= title
  p Welcome to #{title}
  button(id="test")
  
  ul
    each project in projects
      li(data-id="#{project.id}" class=project.status === 1 ? "success" : "error")= project.name

